# Настройка агента 3x-UI

Полное руководство по установке и настройке агента для мультисерверной архитектуры.

## 📋 Оглавление

1. [Архитектура](#архитектура)
2. [Установка агента](#установка-агента)
3. [Генерация mTLS сертификатов](#генерация-mtls-сертификатов)
4. [Настройка агента](#настройка-агента)
5. [Проверка работы агента](#проверка-работы-агента)
6. [Подключение агента к панели](#подключение-агента-к-панели)
7. [Устранение проблем](#устранение-проблем)

---

## Архитектура

```
┌─────────────────────────────────────────────────────┐
│                  Панель (Controller)                │
│              https://panel-ip:2082                  │
│                                                      │
│  • Веб-интерфейс управления                        │
│  • База данных конфигураций                        │
│  • Локальный Xray                                  │
└────────────────┬────────────────────────────────────┘
                 │
                 │ mTLS / JWT
                 │
      ┌──────────┴───────────┐
      │                      │
┌─────▼─────────┐  ┌────────▼────────┐
│   Агент #1    │  │    Агент #2     │
│  :2054 (mTLS) │  │  :2054 (mTLS)   │
│               │  │                 │
│  • API        │  │  • API          │
│  • Xray       │  │  • Xray         │
└───────────────┘  └─────────────────┘
```

---

## Установка агента

### 1. На сервере где будет агент:

```bash
# Скачать и запустить установщик
bash <(curl -Ls https://raw.githubusercontent.com/cofedish/3x-UI-agents/main/install.sh)

# Выбрать в меню:
# Опция 2) Install Agent (for remote VPN servers)

# Установщик автоматически:
# - Скачает последнюю версию агента
# - Создаст systemd сервис x-ui-agent
# - Запустит агент на порту 2054 (по умолчанию)
```

### 2. Проверить установку:

```bash
# Проверить статус службы
systemctl status x-ui-agent

# Должно быть: active (running)
```

---

## Генерация mTLS сертификатов

### Вариант 1: Использовать тестовые сертификаты (НЕ для продакшна!)

Тестовые сертификаты уже есть в репозитории:

```bash
# На машине разработки:
cd d:/Projects/3xUI-Agents/3x-ui/test/fixtures/mtls/agent1/

# Файлы:
# - agent.crt  # Клиентский сертификат
# - agent.key  # Приватный ключ
# - ca.crt     # CA сертификат
```

### Вариант 2: Сгенерировать продакшн сертификаты

```bash
# 1. Создать директорию для сертификатов
mkdir -p ~/certs && cd ~/certs

# 2. Сгенерировать CA (Центр сертификации)
openssl genrsa -out ca.key 4096
openssl req -new -x509 -days 3650 -key ca.key -out ca.crt \
  -subj "/C=US/ST=State/L=City/O=Organization/CN=My CA"

# 3. Сгенерировать ключ агента
openssl genrsa -out agent.key 4096

# 4. Создать запрос на сертификат (CSR)
openssl req -new -key agent.key -out agent.csr \
  -subj "/C=US/ST=State/L=City/O=Organization/CN=agent"

# 5. Подписать сертификат агента с помощью CA
cat > agent.ext <<EOF
basicConstraints = CA:FALSE
keyUsage = digitalSignature, keyEncipherment
extendedKeyUsage = clientAuth
subjectKeyIdentifier = hash
authorityKeyIdentifier = keyid,issuer
EOF

openssl x509 -req -in agent.csr -CA ca.crt -CAkey ca.key \
  -CAcreateserial -out agent.crt -days 365 -extfile agent.ext

# 6. Проверить сертификат
openssl verify -CAfile ca.crt agent.crt
# Должно быть: agent.crt: OK
```

### Вариант 3: Использовать скрипт из репозитория

```bash
# На Windows (PowerShell):
cd d:/Projects/3xUI-Agents/3x-ui/test/fixtures/scripts
powershell -ExecutionPolicy Bypass -File gen-prod-certs.ps1

# На Linux:
cd ~/3xUI-Agents/3x-ui/test/fixtures/scripts
bash gen-prod-certs.sh
```

---

## Настройка агента

### 1. Скопировать сертификаты на сервер агента:

```bash
# С локальной машины на удаленный сервер:
scp ~/certs/agent.crt root@agent-server:/etc/x-ui-agent/certs/
scp ~/certs/agent.key root@agent-server:/etc/x-ui-agent/certs/
scp ~/certs/ca.crt root@agent-server:/etc/x-ui-agent/certs/

# Установить права доступа:
ssh root@agent-server "chmod 600 /etc/x-ui-agent/certs/agent.key"
ssh root@agent-server "chmod 644 /etc/x-ui-agent/certs/agent.crt"
ssh root@agent-server "chmod 644 /etc/x-ui-agent/certs/ca.crt"
```

### 2. Настроить переменные окружения агента:

```bash
# На сервере агента:
nano /etc/systemd/system/x-ui-agent.service

# Добавить/изменить переменные:
[Service]
Environment="AGENT_SERVER_ID=agent-001"
Environment="AGENT_SERVER_NAME=VPN Server US-1"
Environment="AGENT_LISTEN_ADDR=0.0.0.0:2054"
Environment="AGENT_AUTH_TYPE=mtls"
Environment="AGENT_CERT_FILE=/etc/x-ui-agent/certs/agent.crt"
Environment="AGENT_KEY_FILE=/etc/x-ui-agent/certs/agent.key"
Environment="AGENT_CA_FILE=/etc/x-ui-agent/certs/ca.crt"
Environment="AGENT_LOG_LEVEL=info"

# Перезагрузить демон и перезапустить службу:
systemctl daemon-reload
systemctl restart x-ui-agent
```

---

## Проверка работы агента

### 1. Проверить что агент запущен:

```bash
# Проверить статус:
systemctl status x-ui-agent

# ✅ Должно быть:
# Active: active (running)
# Process: Running

# Проверить логи:
journalctl -u x-ui-agent -f

# ✅ Должно быть:
# "Agent API server listening on 0.0.0.0:2054"
# "mTLS enabled, CA file: /etc/x-ui-agent/certs/ca.crt"
```

### 2. Проверить что порт открыт:

```bash
# На сервере агента:
ss -tlnp | grep 2054

# ✅ Должно показать:
# LISTEN  0.0.0.0:2054

# Проверить файрвол:
ufw status
# Если порт 2054 закрыт:
ufw allow 2054/tcp
```

### 3. Проверить mTLS (должен отклонять без сертификата):

```bash
# Попытка подключиться БЕЗ сертификата (должна провалиться):
curl https://agent-server-ip:2054/api/health

# ✅ Должна быть ошибка:
# "SSL certificate problem" или "Connection refused"

# Попытка с сертификатом (должна работать):
curl --cacert ca.crt \
     --cert agent.crt \
     --key agent.key \
     https://agent-server-ip:2054/api/health

# ✅ Должен вернуть:
# {"success":true,"msg":"","obj":{"status":"ok"}}
```

---

## Подключение агента к панели

### 1. Получить данные для подключения:

**Вам нужно:**
- **Endpoint:** `https://agent-server-ip:2054`
- **Auth Type:** `mTLS`
- **Auth Data:** Содержимое трех файлов подряд:
  ```
  [Содержимое agent.crt]
  [Содержимое agent.key]
  [Содержимое ca.crt]
  ```

### 2. Подготовить Auth Data:

```bash
# На локальной машине объединить сертификаты:
cat agent.crt agent.key ca.crt > combined-certs.txt

# Скопировать содержимое:
cat combined-certs.txt
# Скопировать весь вывод в буфер обмена
```

### 3. Добавить агента через веб-интерфейс:

1. Открыть панель: `https://panel-ip:2082/путь/panel/servers`
2. Нажать **"Добавить сервер"** (кнопка `+`)
3. Заполнить форму:

   ```
   Имя сервера:        VPN-US-1
   Адрес:              https://91.217.76.87:2054
   Тип аутентификации: mTLS (Рекомендуется)
   Данные аутентификации: [вставить содержимое combined-certs.txt]
   Теги:               us, production
   Включено:           ✓
   ```

4. Нажать **"Подтвердить"**

### 4. Проверить подключение:

1. В таблице серверов найти добавленный сервер
2. Нажать кнопку **"Проверить здоровье"** (иконка сердца ❤️)
3. Должно появиться: **"✅ VPN-US-1: OK"**
4. Статус должен стать: **"Онлайн"** (зеленая точка)

---

## Устранение проблем

### Проблема: Агент не запускается

```bash
# Проверить логи:
journalctl -u x-ui-agent -n 100 --no-pager

# Частые причины:
# 1. Порт занят:
ss -tlnp | grep 2054
# Решение: изменить порт или убить процесс

# 2. Файлы сертификатов не найдены:
ls -la /etc/x-ui-agent/certs/
# Решение: проверить пути в переменных окружения

# 3. Неправильные права доступа:
chmod 600 /etc/x-ui-agent/certs/*.key
chmod 644 /etc/x-ui-agent/certs/*.crt
```

### Проблема: Панель не может подключиться к агенту

```bash
# 1. Проверить файрвол на сервере агента:
ufw status
ufw allow 2054/tcp

# 2. Проверить что агент слушает на правильном IP:
ss -tlnp | grep 2054
# Должно быть 0.0.0.0:2054, а не 127.0.0.1:2054

# 3. Проверить сертификаты:
openssl x509 -in agent.crt -text -noout | grep "Not After"
# Проверить что не истек срок действия

# 4. Проверить подключение вручную:
curl --cacert ca.crt --cert agent.crt --key agent.key \
     https://agent-ip:2054/api/health -v
```

### Проблема: "Health check failed"

```bash
# Проверить логи агента:
journalctl -u x-ui-agent -f

# Проверить что CA сертификаты совпадают:
openssl x509 -in /etc/x-ui-agent/certs/ca.crt -noout -fingerprint
# Сравнить с CA который использует панель

# Проверить что сертификат не самоподписанный:
openssl verify -CAfile ca.crt agent.crt
# Должно быть: agent.crt: OK
```

### Проблема: "SSL certificate problem"

```bash
# Проверить цепочку сертификатов:
cat agent.crt ca.crt > chain.pem
openssl verify chain.pem

# Убедиться что в Auth Data правильный порядок:
# 1. Client certificate (agent.crt)
# 2. Private key (agent.key)
# 3. CA certificate (ca.crt)
```

---

## Переменные окружения агента

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `AGENT_SERVER_ID` | Уникальный ID агента | `agent-1` |
| `AGENT_SERVER_NAME` | Название сервера | `Agent Server` |
| `AGENT_LISTEN_ADDR` | IP:Port для прослушивания | `0.0.0.0:2054` |
| `AGENT_AUTH_TYPE` | Тип аутентификации (`mtls` или `jwt`) | `mtls` |
| `AGENT_CERT_FILE` | Путь к клиентскому сертификату | `/etc/x-ui-agent/certs/agent.crt` |
| `AGENT_KEY_FILE` | Путь к приватному ключу | `/etc/x-ui-agent/certs/agent.key` |
| `AGENT_CA_FILE` | Путь к CA сертификату | `/etc/x-ui-agent/certs/ca.crt` |
| `AGENT_LOG_LEVEL` | Уровень логирования (`debug`, `info`, `warn`, `error`) | `info` |

---

## Быстрый чеклист

- [ ] Агент установлен и запущен (`systemctl status x-ui-agent`)
- [ ] Порт 2054 открыт в файрволе
- [ ] Сертификаты скопированы в `/etc/x-ui-agent/certs/`
- [ ] Права доступа установлены (`chmod 600 *.key`)
- [ ] mTLS настроен и работает (curl test)
- [ ] Агент добавлен в панель через веб-интерфейс
- [ ] Health check проходит успешно
- [ ] Статус агента "Онлайн"

---

## Полезные команды

```bash
# Перезапустить агента:
systemctl restart x-ui-agent

# Посмотреть логи в реальном времени:
journalctl -u x-ui-agent -f

# Проверить конфигурацию:
systemctl cat x-ui-agent

# Проверить открытые порты:
ss -tlnp

# Проверить сертификаты:
openssl x509 -in agent.crt -text -noout

# Тест подключения с панели:
curl -k https://agent-ip:2054/api/health
```

---

## Дополнительно

- Документация API агента: [AGENT-API.md](./AGENT-API.md)
- Примеры использования: [examples/](../examples/)
- Скрипты генерации сертификатов: [test/fixtures/scripts/](../3x-ui/test/fixtures/scripts/)
